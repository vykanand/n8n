version: '3.8'
services:
  n8n:
    build: .
    container_name: n8n
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=false
      - TZ=UTC
      - NODE_ENV=production
    volumes:
      - ./data:/home/node/.n8n
    restart: unless-stopped
